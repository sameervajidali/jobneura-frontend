export default function QuizSidebar({ filters, onChange }) {
    return (
      <aside className="w-1/5 p-4 bg-white border-r">
        {/* TODO: SearchBox, CategoryFilter, TopicFilter, LevelFilter */}
        <p>Sidebar filters here</p>
      </aside>
    );
  }
  